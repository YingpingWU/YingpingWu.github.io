<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>Please play the game</h1>
        </div>
        <div class="col-12">
            {{#if post}}
                The computer chose the number {{computerNumber}}.
                User 1 chose {{userNumber1}} and User 2 chose {{userNumber2}}.
                {{winningUser}} is the winner.
            {{/if}}
            {{#unless post}}
                <form action="/game" method="post">
                    <div class="form-group">
                        <label for="userNumbe1">Number for user 1</label>
                        <input type="text" class="form-control" id="userNumber1" aria-describedby="emailHelp" name="userNumber1" placeholder="Enter your number">
                    </div>
                    <div class="form-group">
                        <label for="userNumber2">Number for user 2</label>
                        <input type="text" class="form-control" id="userNumber2" aria-describedby="emailHelp" name="userNumber2" placeholder="Enter your number">
                    </div>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>
            {{/unless}}
        </div>
    </div>
</div>
              